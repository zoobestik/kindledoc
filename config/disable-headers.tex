\makeatletter
% neuter the common styles in case the class or header.tex select them
\let\ps@plain\ps@empty
\let\ps@headings\ps@empty
\let\ps@fancy\ps@empty
\makeatother
\AtBeginDocument{%
  \pagestyle{empty}% no headers/footers from here on
  \thispagestyle{empty}% also remove on the first page (after \maketitle)
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
